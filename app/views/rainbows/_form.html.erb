<%= form_for(@rainbow) do |f| %>
  <% if @rainbow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rainbow.errors.count, "error") %> prohibited this rainbow from being saved:</h2>

      <ul>
      <% @rainbow.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :pretty %><br />
    <%= f.check_box :pretty %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
